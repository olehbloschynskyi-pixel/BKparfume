{
  "name": "bkparfume",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:js": "node scripts/build-app-js.js",
    "catalog:add": "node scripts/import-product.js",
    "catalog:normalize": "node scripts/normalize-product-data.js",
    "catalog:build": "npm run catalog:normalize && npm run build:js && npm run generate:products && npm run generate:sitemap",
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate:products": "node scripts/generate-product-pages.js",
    "generate:sitemap": "node scripts/generate-sitemap.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cssnano": "^7.1.5",
    "postcss": "^8.5.10",
    "postcss-cli": "^11.0.1",
    "terser": "^5.47.1"
  }
}
