---
deployment:
  tasks:
    - export DEPLOYPATH=/home/pesbipdg/public_html/
    - /usr/bin/rsync -av --exclude='.git' --exclude='.cpanel.yml' --exclude='node_modules' --exclude='.DS_Store' ./ $DEPLOYPATH
