---
deployment:
  tasks:
    - export DEPLOYPATH=/home/pesbipdg/public_html/
    - /usr/bin/rsync -avz --timeout=30 --delete --exclude='.git' --exclude='.cpanel.yml' --exclude='node_modules' ./ $DEPLOYPATH
